name: _LAUNCH TEST — root workflow from PR

on:
  workflow_dispatch:
    inputs:
      FRONTEND_DOCKER_TAG: { required: true, description: "Frontend tag to test" }
      BACKEND_DOCKER_TAG:  { required: true, description: "Backend tag to test" }

jobs:
  run-root:
    uses: Rajeshgupta123456789/travelblog-project/.github/workflows/travelblog-root.yml@<PR_BRANCH_ROOT>
    with:
      FRONTEND_DOCKER_TAG: ${{ inputs.FRONTEND_DOCKER_TAG }}
      BACKEND_DOCKER_TAG:  ${{ inputs.BACKEND_DOCKER_TAG }}
    secrets: inherit
